#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<bool> hoffman(string str);

int main(){
	string s;
	cin >> s;
	vector<bool> bin = hoffman(s);
}

vector<bool> hoffman(string str){
	vector<bool> bin;
}
